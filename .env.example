# ====================================================================
# flowgpt-2api 配置文件模板
# ====================================================================
#
# 请将此文件重命名为 ".env" 并填入您的凭证。
#

# --- 核心安全配置 (必须设置) ---
# 用于保护您 API 服务的访问密钥。
API_MASTER_KEY=change-me-to-a-secure-key

# --- 部署配置 (可选) ---
# Nginx 对外暴露的端口
NGINX_PORT=8088

# --- FlowGPT 凭证 (必须设置) ---
# 请从浏览器开发者工具(F12)的网络(Network)标签页中，
# 找到任意一个对 "prod-backend-k8s.flowgpt.com" 的请求，
# 从其请求头(Request Headers)中找到 "authorization" 字段，
# 并复制 "Bearer " 之后的那一长串 token 字符串，粘贴在此处。
#
# 错误示例: Bearer eyJhbGciOiJSUzI1Ni...
# 正确示例: eyJhbGciOiJSUzI1Ni...
#
FLOWGPT_BEARER_TOKEN=在此处粘贴您的Token字符串

# --- 签名配置 (通常无需修改) ---
# 用于生成请求签名的设备 ID。已预填了通用值。
# 如果 API 验证失败，请从您自己的网络请求头中找到 "x-flow-device-id" 的值并更新。
FLOWGPT_DEVICE_ID="aORT9gbujH92iIVYYVcTE"
